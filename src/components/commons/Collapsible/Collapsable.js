import React from 'react';
import Accordion from 'react-native-collapsible/Accordion';

const Collapsable = ({title, body, icon}) => {
  return <Accordion />;
};

Collapsable.propTypes = {};

export default Collapsable;
